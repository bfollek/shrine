(ns shrine.home
  (:require ["package:flutter/material.dart" :as m]
            [cljd.flutter :as f]))

(def home-page
  (f/widget
   (m/Scaffold
    .body
    (m/Center
     .child (m/Text "You did it!")
     .resizeToAvoidBottomInset false))))
