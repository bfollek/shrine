(ns shrine.model.product)

(defn asset-name
  [product]
  (str (:id product) "-0.jpg"))

(defn asset-package
  [_product]
  "shrine-images")

(defn to-string
  [product]
  (str (:name product) " (id=" (:id product) ")"))
